# Changelog

## Unreleased

- Flatten the Maya package into root `scripts/` and `plug-ins/` directories.
- Remove the redundant bootstrap package.
- Remove the unnecessary Maya module file.

## 2.0.1

- Preserve the existing command plug-in implementation.
- Add a Maya module package and documented bootstrap entry point.
